# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# https://goreleaser.com
version: 2

project_name: llml

before:
  hooks:
    - go mod tidy

builds:
  - id: llml
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    main: ./cmd/llml
    binary: llml
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X=main.version={{.Version}}

archives:
  - id: default
    ids:
      - llml
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"

homebrew_casks:
  - name: llml
    ids:
      - default
    binaries:
      - llml
    homepage: https://github.com/flyingnobita/llml
    description: Terminal UI to discover local models and launch llama-server or vLLM
    license: MIT
    skip_upload: '{{ eq (index .Env "GITHUB_TOKEN") "" }}'
    repository:
      owner: flyingnobita
      name: llml
      branch: "homebrew-cask-{{ .Version }}"
      token: "{{ .Env.GITHUB_TOKEN }}"
      pull_request:
        enabled: true
        base:
          owner: flyingnobita
          name: llml
          branch: main
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com

scoops:
  - name: llml
    ids:
      - default
    homepage: https://github.com/flyingnobita/llml
    description: Terminal UI to discover local models and launch llama-server or vLLM
    license: MIT
    skip_upload: '{{ eq (index .Env "SCOOP_BUCKET_GITHUB_TOKEN") "" }}'
    repository:
      owner: flyingnobita
      name: scoop-bucket
      token: "{{ .Env.SCOOP_BUCKET_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com

winget:
  - name: llml
    ids:
      - default
    publisher: Flying Nobita
    publisher_url: https://github.com/flyingnobita
    publisher_support_url: https://github.com/flyingnobita/llml/issues
    package_identifier: FlyingNobita.llml
    short_description: Terminal UI to discover local models and launch llama-server or vLLM
    license: mit
    homepage: https://github.com/flyingnobita/llml
    skip_upload: '{{ eq (index .Env "WINGET_GITHUB_TOKEN") "" }}'
    repository:
      owner: flyingnobita
      name: winget-pkgs
      branch: "llml-{{ .Version }}"
      token: "{{ .Env.WINGET_GITHUB_TOKEN }}"
      pull_request:
        enabled: true
        base:
          owner: microsoft
          name: winget-pkgs
          branch: master
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
